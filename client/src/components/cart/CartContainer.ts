import styled from 'styled-components';
import { PageContent } from '../shared';

const CartContainer = styled(PageContent)`
  display: grid;
  grid-template-columns: 66% 1fr;
  grid-column-gap: 30px;
`;

export default CartContainer;
