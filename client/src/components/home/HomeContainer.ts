import styled from 'styled-components';
import { PageContent } from '../shared';

const HomeContainer = styled(PageContent)`
  display: flex;
  flex-direction: column;
  row-gap: 40px;
`;

export default HomeContainer;
